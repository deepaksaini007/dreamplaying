import { DreamTimePipe } from './dream-time.pipe';

describe('DreamTimePipe', () => {
  it('create an instance', () => {
    const pipe = new DreamTimePipe();
    expect(pipe).toBeTruthy();
  });
});
